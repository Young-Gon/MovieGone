import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
    title: {
        fontSize: 18,
        fontWeight: 'bold',
        marginLeft: 20,
        marginTop: 20
    },
});

export default commonStyles;